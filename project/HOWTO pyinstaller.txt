Run this command from within the project catalouge (Windows):

pyinstaller --windowed --icon=gca.ico --name="GCA Analysis Tool" main.py

The gca.ico must then manually be copied into the project/dist/GCA-Analysis-Tool directory.
